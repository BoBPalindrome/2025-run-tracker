{
  "name": "calendar-heatmap-project",
  "version": "1.0.0",
  "description": "Calendar heatmap visualization",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "# Add build step if needed",
    "vercel-build": "# Add vercel-specific build step if needed"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.2",
    "path": "^0.12.7"
  }
} 